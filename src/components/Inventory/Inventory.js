import React from 'react';


const Inventory = () => {
    const handleAddInventory=()=>{
        
    }

    return (
        <div>
            <h1>Add More products to sell more</h1>
            <button onClick={handleAddInventory} >Add inventory</button>
        </div>
    );
};

export default Inventory;